# Changelog

## [Unreleased]
- Established initial repo skeleton and migrated the roadmap.
- Added stable vs. volatile reward schedules plus the epsilon-greedy simulator in `src/rl_sim`.
- Added severity-group helper (`simulate_severity_groups`) with default low/high profiles.
- Added severity plotting script (`scripts/plot_severity.py`) and figure notes in `docs/figures.md`.

